// Accessor functions

// 1. indexOf
var names = ['one', 'two', 'three'];
var pos = names.indexOf('one');

if (pos >= 0) {
    console.log("found " + "one" + " at pos: " + pos);
} else {
    console.log('one' + ' not found in array: ', names);
}

// 2. toString()

// 3. concat(), splice()
new_string = old_string.concat(another_old_string);
new_spliced_string = old_string(pos_to_start_splicing, number_of_el_to_splice);

